# Production URL – used for canonical URLs, sitemap, Open Graph, and JSON-LD.
# Live domain: https://chaptersaurchai.com
NEXT_PUBLIC_SITE_URL=https://chaptersaurchai.com

# Backend API base URL (admin login, settings, page content). Set so the app can reach the API.
NEXT_PUBLIC_API_URL=http://localhost:5000
