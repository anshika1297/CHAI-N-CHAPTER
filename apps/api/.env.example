NODE_ENV=development
PORT=5000
FRONTEND_URL=http://localhost:3000
# For links in emails (images, unsubscribe). Must be reachable by recipients. Set to your live site URL (e.g. https://yoursite.com) so email images load. Defaults to FRONTEND_URL.
# PUBLIC_SITE_URL=https://yoursite.com
# Must include database name so the API uses your chai-n-chapter DB (not default "test").
MONGODB_URI=mongodb+srv://thebookishvoyager:Reader%4012@chai-n-chapter.gvy1ste.mongodb.net/chai-n-chapter
JWT_SECRET=your-secret-key-change-in-production

# For seed-admin script (create first admin user). Copy this file to .env and set these there.
ADMIN_EMAIL=thebookishvoyager@gmail.com
ADMIN_PASSWORD="Reader@12"

# Optional: SMTP for welcome email when someone subscribes. If not set here, you can configure SMTP from Admin → Subscriber emails (stored in DB).
# Gmail: use an App Password (Google Account → Security → 2-Step Verification → App passwords), not your normal password.
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=the.bookish.voyager@gmail.com
SMTP_PASS=tbyadwqikmbdkapp
SMTP_FROM="Chai & Chapter <the.bookish.voyager@gmail.com>"